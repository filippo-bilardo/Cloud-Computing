server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: java-docker-api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
